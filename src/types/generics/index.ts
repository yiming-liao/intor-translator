// key
export type { Key, LocalizedKey, ScopedKey } from "./key";

// pre-key
export type { PreKey, LocalizedPreKey } from "./pre-key";

// value
export type { Value, LocalizedValue, ScopedValue } from "./value";

// replacement
export type {
  Replacement,
  LocalizedReplacement,
  ScopedReplacement,
} from "./replacement";

// utils
export type {
  GeneratePaths,
  AtPath,
  IfLocaleMessages,
  IfMessageObject,
} from "./utils";
