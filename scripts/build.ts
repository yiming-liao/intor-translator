import { run } from "./run";

// ----------------------------------------------------------
// clean up
// ----------------------------------------------------------
run("rm -rf dist");

// ----------------------------------------------------------
// tsup
// ----------------------------------------------------------
run("tsup");

// ----------------------------------------------------------
// dts
// ----------------------------------------------------------
run("tsc -p tsconfig.build.json");
