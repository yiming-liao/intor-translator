# HTML Example

A minimal browser demo showing how to use **Intor Translator** together with
`parseRichMessage` to render rich, semantic messages in the browser.

## Run

```bash
npm run examples:html:rich
```
